# 국토종합진단지수 대시보드 — 기능 명세서 및 정보구조(IA)

- 문서 버전: v1.0 (2026-07-26)
- 대상 산출물: `docs/index.html` (단일 자기완결형 HTML)
- 개정 사유: 3열 배치가 `조작 → 대상 → 판독` 순이라 조작 결과를 읽으려면 시선이 화면을 두 번 가로질러야 했다. 판독부를 조작부 옆으로 당기고, 필요 없을 때 접을 수 있게 한다.

---

## 1. 설계 원칙

산업디자인의 조작반(control panel) 설계 관례를 화면에 옮긴 것이다. 계기판·공작기계 조작부에서 통용되는 규칙을 근거로 삼았다.

| 번호 | 원칙 | 내용 | 이 대시보드에서의 적용 |
|---|---|---|---|
| P1 | 조작-표시 근접성 | 조작부와 그 결과를 표시하는 계기는 물리적으로 붙어 있어야 한다. 멀면 조작 후 결과 확인까지의 시선 이동이 오류와 피로를 만든다 | 표준화 방법 선택기(조작)와 방법별 CI·순위 표(표시)를 인접한 두 열에 배치 |
| P2 | 인과 순서 = 읽기 순서 | 좌→우 읽기 문화권에서는 원인이 왼쪽, 결과가 오른쪽에 놓일 때 인지 비용이 가장 낮다 | `무엇을 볼지 고른다 → 숫자로 확인한다 → 공간에서 확인한다` 순으로 열을 배열 |
| P3 | 단일 지배 요소 | 한 화면에는 시선을 붙잡는 지배 요소가 하나여야 한다 | 지도를 가장 넓은 우측 열에 두고 가변폭으로 확장. 나머지는 고정폭 |
| P4 | 가역적 점진 노출 | 상세 정보는 필요할 때 펴고 아닐 때 접을 수 있어야 하며, 접은 상태에서도 되돌아갈 손잡이가 보여야 한다 | 통계창 접기/펼치기. 접힌 상태에서 세로 레일이 남아 위치를 알림 |
| P5 | 상태 가시성 | 현재 어떤 모드인지 시스템이 항상 명시해야 한다 | 상단 브레드크럼에 선택 시군구·부문·표준화 방법·지도 지표를 상시 표시 |
| P6 | 모드리스 조작 | 조작이 별도 모드로 진입하지 않고 즉시 반영되어야 한다 | 방법 전환 시 확인 단계 없이 지도·표·차트가 동시에 갱신 |

---

## 2. 사용자와 과업

| 사용자 | 목적 | 대표 질문 |
|---|---|---|
| 지수 설계자 | 표준화 방법 선택의 타당성 확보 | 방법을 바꾸면 순위가 얼마나 흔들리는가. 어떤 지역이 특히 불안정한가 |
| 정책 담당자 | 자기 지역의 위치 파악 | 우리 시군구는 몇 위인가. 그 순위는 믿을 만한가 |
| 검토자 | 산출 과정 검증 | 이 CI 값은 어떤 원자료에서 어떤 공식으로 나왔는가 |

| ID | 핵심 과업 | 성공 기준 |
|---|---|---|
| T1 | 표준화 방법을 바꿔가며 지도 변화 관찰 | 전환 후 1초 이내 지도 재채색, 변화량이 수치로 제시됨 |
| T2 | 두 방법을 동시에 비교 | 좌우 지도가 같은 시점·같은 축척으로 나란히 보임 |
| T3 | 특정 시군구의 계산 과정 추적 | 원자료부터 최종 순위까지 한 화면에서 끊기지 않고 읽힘 |
| T4 | 민감 지역 선별 | 민감(high) 지역만 남기는 필터, 상위 목록 제공 |
| T5 | 원자료 전량 확인·반출 | 40개 컬럼 229행 조회, 정렬, CSV 저장 |

---

## 3. 기능 명세

상태 열의 표기는 `기본값 → 가능한 값`이다.

### 3.1 조작부 (F1x)

| ID | 기능 | 입력 | 출력·반응 | 상태 |
|---|---|---|---|---|
| F11 | 부문 선택 | S1 공간구조효율성 / S8 지역사회건강도 | 지도·통계창·데이터표 전체 갱신 | S1 → S1, S8 |
| F12 | 표준화 방법 선택 | 4개 방법 버튼 | 지도 재채색, 계산표 강조열 이동 | Min-Max → MM, DI, PR, LG |
| F13 | 전환 영향 예고 | (자동) | 현재 방법 대비 색 등급이 바뀌는 시군구 수를 버튼별로 표시 | 읽기 전용 |
| F14 | 지도 지표 선택 | 순위 / CI / SSI_camp / 순위차 | 지도 색상 스케일·범례 교체 | 전국 순위 |
| F15 | 민감 지역만 보기 | 토글 | 비민감 지역을 흐리게 처리 | 꺼짐 |
| F16 | A/B 비교 보기 | 토글 | 단일 지도 → 좌우 2분할 지도, 팬·줌 동기화 | 꺼짐 |
| F17 | 통계창 접기 | 버튼 | 통계창 폭 0, 지도 폭 확장, 세로 레일 노출 | 펼침 |

### 3.2 판독부 · 통계창 (F2x)

| ID | 기능 | 내용 | 근거 |
|---|---|---|---|
| F21 | 선택 시군구 요약 | 두 진영 대표 순위와 그 차이(SSI_camp), 4개 방법별 CI·순위 | 지침서 9장 |
| F22 | 계산 과정 추적 | 원자료 → 방향반전 → 표준화 → CI → 순위. 지표 단위 순위가 4방법 동일함을 표에 명시 | 단조변환 성질 |
| F23 | 순위 이동 | 방법 간 순위 이동 범프 차트 | — |
| F24 | 분포 비교 | 방법별 CI 히스토그램 + 10개 값 예시 | 지침서 6.4 |
| F25 | 원자료 지표 | 부문 내 지표 원값·단위·방향 | 컬럼메타데이터 |
| F26 | 전국 민감도 산점도 | MinMax순위 × PctRank순위, 대각선 이탈이 민감도 | — |
| F27 | 민감 상위 목록 | SSI_camp 내림차순, 클릭 시 지도 연동 | — |

### 3.3 대상부 · 지도 (F3x)

| ID | 기능 | 내용 |
|---|---|---|
| F31 | 단계구분도 | 229개 시군구, 선택 지표 기준 채색 |
| F32 | 민감 지역 표시 | 민감(high) 지역 빨간 테두리 |
| F33 | 선택·호버 연동 | 지도 클릭 → 통계창 갱신. 목록·산점도 클릭 → 지도 강조 |
| F34 | 범례 | 색 스케일과 양끝 라벨, 지표 변경 시 교체 |
| F35 | A/B 분할 | 두 방법 지도 동시 표시, 뷰포트 동기화 |

### 3.4 전역 (F4x)

| ID | 기능 | 내용 |
|---|---|---|
| F41 | 전체 데이터표 | 40컬럼 × 229행, 정렬, 컬럼 설명 툴팁, CSV 내보내기 |
| F42 | 용어·방법론 사전 | 핵심 용어 / 4개 방법 / 컬럼사전 40개 |
| F43 | 상태 공유 | 부문·방법·지표·선택지역·비교모드를 URL 해시에 반영 |
| F44 | 상단 요약 | SSI_camp 평균·최대·민감 지역 수 |

---

## 4. 정보구조 (IA)

### 4.1 계층

```
국토종합진단지수 대시보드
├─ 1 조작 (무엇을 볼 것인가)
│  ├─ 1.1 진단 대상    · 부문 선택
│  ├─ 1.2 진단 방법    · 표준화 방법 · 전환 영향 예고
│  ├─ 1.3 표현 방식    · 지도 지표 · 민감 지역 필터
│  └─ 1.4 비교 모드    · A/B 보기 · 통계창 접기
├─ 2 판독 (숫자로 확인)
│  ├─ 2.1 지금 무엇을 보고 있는가   · 브레드크럼
│  ├─ 2.2 이 지역의 결과            · 요약 · 계산 과정
│  ├─ 2.3 방법이 만든 차이          · 순위 이동 · 분포 비교
│  └─ 2.4 전국 맥락                 · 산점도 · 민감 상위 목록
├─ 3 대상 (공간에서 확인)
│  └─ 3.1 전국 지도 / A/B 분할 지도
└─ 4 전역 참조
   ├─ 4.1 전체 데이터표
   └─ 4.2 용어 · 방법론 사전
```

### 4.2 화면 구역 배치

개정 전후를 비교하면 다음과 같다.

| 구역 | 개정 전 | 개정 후 |
|---|---|---|
| 좌 | 조작부 292px | 조작부 292px (변동 없음) |
| 중 | 지도 (가변) | **판독부 366px (접기 가능)** |
| 우 | 판독부 350px | **지도 (가변, 최소 420px)** |

개정 후 배치는 원칙 P2의 인과 순서와 일치한다. 왼쪽에서 조건을 고르고, 가운데에서 그 조건이 만든 숫자를 읽고, 오른쪽에서 그 숫자가 공간에 어떻게 분포하는지 본다. 통계창을 접으면 가운데가 비면서 지도가 조작부 바로 옆까지 확장되어, 지도만 크게 보는 사용에도 대응한다.

### 4.3 치수 사양

| 항목 | 값 | 비고 |
|---|---|---|
| 헤더 높이 | 58px | 고정 |
| 바디 여백 | 14px | 상하좌우 및 열 간격 |
| 조작부 폭 | 292px | 고정 |
| 판독부 폭 | 366px | 고정. 접힘 시 0 |
| 접힘 레일 폭 | 34px | 세로 라벨 + 펼치기 버튼 |
| 지도 최소 폭 | 420px | 이하로 좁아지면 반응형 전환 |
| 카드 모서리 | 16px | 조작부·지도 |
| 카드 모서리 | 12px | 판독부 내부 카드 |

### 4.4 반응형

| 구간 | 배치 |
|---|---|
| 1400px 이상 | 3열 전개 (조작 · 판독 · 지도) |
| 1180 ~ 1400px | 3열 유지, 판독부 폭 330px로 축소 |
| 1180px 미만 | 세로 적층. 조작 → 지도(70vh) → 판독 순. A/B 지도는 상하 2분할 |

---

## 5. 상호작용 상태표

| 현재 상태 | 조작 | 결과 상태 | 화면 변화 |
|---|---|---|---|
| 통계창 펼침 | 접기 버튼 클릭 | 통계창 접힘 | 판독부 폭 366→0, 지도 좌측으로 확장, 좌측에 세로 레일 등장 |
| 통계창 접힘 | 레일 클릭 | 통계창 펼침 | 역방향. 스크롤 위치와 선택 상태 유지 |
| 단일 지도 | A/B 토글 | 분할 지도 | 지도 영역만 2분할. 조작부·판독부 불변 |
| 분할 지도 | 통계창 접기 | 분할 지도 + 접힘 | 두 지도 모두 확장 |
| 임의 상태 | 표준화 방법 전환 | 방법만 변경 | 지도 재채색, 계산표 강조열 이동, 선택 시군구 유지 |
| 임의 상태 | 지도 클릭 | 선택 시군구 변경 | 판독부 전체 갱신. 접혀 있으면 자동으로 펼치지 않음 |

접힘 상태에서 지도를 클릭해도 통계창을 강제로 펼치지 않는다. 사용자가 명시적으로 접은 것을 시스템이 되돌리면 조작 주도권이 흔들리기 때문이다(P4). 대신 레일에 선택된 지역명을 세로로 표시해 갱신되었음을 알린다.

---

## 6. 제약과 비목표

| 항목 | 내용 |
|---|---|
| 제약 | 단일 HTML 파일. 빌드 산출물이 4.5MB를 크게 넘지 않아야 함 |
| 제약 | 지도 폴리곤은 캔버스 렌더링(preferCanvas). DOM 선택자로 개별 폴리곤을 다룰 수 없음 |
| 제약 | 데이터는 정적 스냅샷. 실시간 갱신 없음 |
| 비목표 | 가중치 사용자 조정 — 현재 동일가중 고정 |
| 비목표 | 부문 확장 — 현재 S1·S8 2개. xlsx에 컬럼이 추가되면 자동 인식 |
| 비목표 | 모바일 전용 레이아웃 — 1180px 미만은 적층 대응까지만 |
