# 인천 1인가구·세대구조 대시보드 — 작업 이력

- 대상: 인천 10개 시군구 · 정적 단일 HTML(GitHub Pages)
- 구조: 정부 원본 파일 → Python 전처리 → 정적 JSON → React(Vite) → 단일 index.html
- 정리일: 2026-07-24

## 구축 순서

| 순서 | 작업 | 내용 |
|---|---|---|
| 1 | 기획 | 인천 한정, API 대신 공식 파일(CSV·SHP) 사용, React+Vite, 서버 없이 단일 HTML |
| 2 | 데이터 파이프라인 | `extract.py`로 KOSIS 가구(2023)·행안부 주민등록(2025)·경계 SHP 정제 → 정적 JSON |
| 3 | 지도 전환 | Leaflet+CARTO 실지도로 교체, 읍면동→시군구 병합, 지도·순위·필터 양방향 연동 |
| 4 | 디자인·정리 | AR6 프로젝트와 톤 통일, AI가 지어낸 해석·임의분류 제거 |
| 5 | 격자 히트맵(표본) | 구 확대 시 밀집도 히트맵 표시 (실데이터 없어 표본, 범례에 '표본' 명시) |
| 6 | 프론트 실시간 연산 | 가중 집중지수 슬라이더, 산점도 4사분면 유형 분석 |
| 7 | 정보구조·사용성 | 출처 모달, 사이드바 기능 그룹화, 3열 레이아웃, '1인가구 집중지수'로 개명 |
| 8 | 범례 버그 수정 | 타일에 가려지던 범례를 상위 레이어로 올림 |
| 9 | 유형화·공유 | 지역 유형화 + URL로 화면 상태 저장·복원(링크 복사) |
| 10 | 실제 SGIS 격자 연동 | 표본 격자를 실제 SGIS 1km 통계(2024)로 교체 |
| 11 | 유형화 방법 교체 | 시군구 10개엔 k-means가 부적절 → 평균 기준 규칙기반 유형화로 대체 |

## 최근 수정 이력 (2026-07-24)

실데이터 연동 과정에서 두 번 어긋났고 아래처럼 복구했습니다.

| 문제 | 원인 | 조치 |
|---|---|---|
| 지역 경계가 네모 박스로 바뀜 | 소스 전달 zip에 진짜 경계가 아닌 테스트용 박스 파일이 섞임 | git 히스토리(`04f5e23`)의 실제 경계(1.27MB) 복원 |
| 격자가 인천 밖(부천·시흥)까지 퍼짐 | '인천 안' 판정 기준이 위의 박스 경계였음 | 실제 경계로 재필터 → 526셀 전부 인천 내부 |
| git 명령이 막힘 | `index.lock` 잔존 | 잠금 파일 제거 후 재실행 |
| "가운데가 빈다" 문의 | 오류 아님 | 바다·인천공항·미개발지 + SGIS 소규모 셀 비공개(합계의 약 76% 표출) |
| 군집분석(k-means)이 부적절 | 시군구 10개는 표본이 너무 작아 군집이 불안정, 옹진군이 "청년형"으로 오분류 | k-means 제거 → **1인가구율 × 고령1인비중, 인천 평균 기준 규칙기반 유형화**로 교체(산점도 ④와 동일 규칙, 투명·검증가능) |

## 현재 상태

- 경계: 실제 인천 시군구 모양(해안선·섬 포함)
- 격자: 실제 SGIS 1km 526셀, 전부 인천 경계 내부
- 로컬·원격 모두 최신 커밋(`8f8aaaa`) 동기화 완료

## 남은 개선 여지 (데이터 추가 필요)

공간통계 핫스팟(Gi*·LISA), 시설 접근성, 주거유형 교차, 시계열·장래추계, 인구이동

---

# 국토종합진단지수 · 표준화 방법 민감도 대시보드 — 작업 이력

- 대상: 전국 229개 시군구 · 정적 단일 HTML(GitHub Pages)
- 구조: `파일럿_분석결과.xlsx`(분석결과 + 컬럼메타데이터 2개 시트) → Python 정제 → 정적 JSON → React(Vite) → 단일 index.html
- 방법론: `표준화_방법론_및_민감도_진단_지침서_v2.pdf`
- 정리일: 2026-07-26

## 구축 순서

| 순서 | 작업 | 내용 |
|---|---|---|
| 1 | 주제 전환 | 인천 1인가구 대시보드에서 국토종합진단지수 표준화 민감도로 주제 변경. 지도·레이아웃·디자인 토큰은 재사용, 데이터층은 전면 교체 |
| 2 | 자료 판독 | xlsx 2개 시트 판독 — `분석결과` 229행 × 40열, `컬럼메타데이터` 40행 × 7열. 요청서의 "225개"는 실제 229개로 확인 |
| 3 | 공식 역산 검증 | 원자료만으로 4개 방법의 CI를 다시 계산해 xlsx의 CI 컬럼과 대조. 2부문 × 4방법 8개 조합 전부 최대오차 0.0005 미만 일치 |
| 4 | 데이터 빌드 | `build_data.py`로 40개 컬럼 전량 + 컬럼메타데이터를 손실 없이 `ssi.json`으로 변환 |
| 5 | 전국 경계 | 시군구 경계 GeoJSON 229개 매칭·단순화 (`sigungu_geo.json`) |
| 6 | 방법 전환 UI | 표준화 방법 4개를 두 진영으로 묶어 선택기 구성. 방법 전환 시 색 등급이 바뀌는 시군구 수를 버튼에 미리 표시 |
| 7 | A/B 지도 비교 | 두 방법의 지도를 좌우 동시 표시, 팬·줌 동기화 |
| 8 | 계산 과정 노출 | 원자료 → 방향반전 → 표준화 → CI → 순위를 단계별 표로 제시. 지표 1개 순위는 4방법 동일함을 표에서 직접 확인 가능하게 함 |
| 9 | 분포·순위 이동 | 방법별 CI 히스토그램, 순위 범프 차트, MinMax순위 × PctRank순위 산점도 |
| 10 | 전체 데이터표 | 40열 × 229행 전량 모달. 정렬·CSV 내보내기·컬럼메타데이터 툴팁 |
| 11 | 용어·방법론 사전 | 핵심 용어 / 4개 표준화 방법 / 컬럼사전 40개 3개 탭 |
| 12 | 헤드리스 검증 | Playwright로 지도 재채색·A/B 지도 2개·데이터표 40열 229행·콘솔오류 0 확인 |
| 13 | 배포 준비 | 인천 저장소와 분리된 신규 저장소 `ssi-camp-dashboard` 구성, `docs/` Pages 배포본 포함해 로컬 커밋 |

## 방법론 정리

| 항목 | 내용 |
|---|---|
| 표준화 4종 | Min-Max `(x−min)/(max−min)×100` · 거리기반 `x/평균×100` · 백분위순위 `평균순위/N×100` · 로지스틱 `100/(1+exp(−z))` |
| 제외 | LQ(입지지수)는 거리기반 × 100과 수학적으로 동일해 제외 |
| 진영 구분 | 간격보존형(MinMax·거리기반·로지스틱) vs 순위전용형(백분위순위) |
| 방향 −1 처리 | 표준화 이전에 `x′ = max + min − x` 로 반전 |
| CI | 부문 내 지표 표준화값의 단순 산술평균(동일가중) |
| 최종 민감도 | `SSI_camp = |순위(MinMax) − 순위(PctRank)|`, 부문 내 80백분위 이상이면 민감구분 high |

## 확인된 사실

| 항목 | 값 |
|---|---|
| 지표 1개 단위 순위 | 4개 방법 모두 동일 (단조변환, Spearman = 1.000) |
| 방법 차이 발생 지점 | 지표를 합치는 CI 단계에서만 발생 |
| 순위 10계단 이상 이동 | 229개 중 35개 (약 16%) |
| 방법 전환 시 색 등급 변경 | 45개 시군구 (MinMax → PctRank 기준) |
| 로지스틱 표준편차 | 표본표준편차(ddof=1) 사용 시 xlsx와 일치 |

## 작업 중 걸린 문제

| 문제 | 원인 | 조치 |
|---|---|---|
| A/B 비교 지도가 세로로 무너짐 | 신규 클래스 `.cmp-bar`가 기존 스타일시트의 동명 규칙(`height:100%`)과 충돌 | 신규 클래스 6개를 `abm-` 접두사로 개명. 이후 신규·기존 선택자 교집합 감사 절차 추가 |
| 지도 재채색을 자동 검증할 수 없음 | 지도가 `preferCanvas` 모드라 SVG 폴리곤이 존재하지 않음 | 방법 전환 전후 캔버스 스크린샷 픽셀 비교로 전환 |
| 검증 스크립트가 엉뚱한 모달을 염 | `.src-btn`이 용어사전 버튼과 데이터표 버튼 양쪽에 걸림 | 텍스트 선택자(`text=전체 데이터표`)로 교체 |
| 콘솔오류 54건 | 컨테이너에 네트워크가 없어 지도 타일·웹폰트 CDN 요청 실패 | 네트워크 오류만 필터링 → 실제 코드 오류 0건 확인 |
| 계산 과정 표가 패널 밖으로 넘침 | 7열 구성이 350px 중앙 패널보다 넓음 | 원자료·지표순위를 첫 열 안으로 접어 5열 고정폭 표로 재구성 |

## 2차 취합 자료 반영 및 화면 재구성 (2026-07-28)

2차 취합 자료가 들어오면서 부문이 2개에서 10개로 늘었고, 같은 지표가 여러 연도로 함께 들어왔습니다. 지표마다 선택 상자를 하나씩 두던 방식으로는 감당이 되지 않아 화면 구조를 다시 짰습니다.

| 순서 | 작업 | 내용 |
|---|---|---|
| 1 | 자료 재적재 | `build_data2.py`로 2차 취합본 재정제. 10개 부문 중 자료가 들어온 6개(S1·S2·S7·S8·S9·S10) 가동, 나머지 4개(S3·S4·S5·S6)는 이름과 예정 지표 수만 표시하고 잠금 |
| 2 | 자료 구조 변경 | `ssi.json`에 계산 결과 대신 원값과 메타데이터만 담고, 표준화·CI·순위는 화면에서 실시간 계산. 229행 × 지표-연도 98열 |
| 3 | 지표 고르기 창 | 부문 목록 · 지표 카드 · 담은 목록 3칸 구성의 별도 창 신설. 지표 설명·단위·산식·출처를 보고 고르며, 같은 지표의 연도를 골라 담을 수 있음 |
| 4 | 지도 이름표 | 시군구 이름을 지도 위에 직접 표시. 면적 큰 지역부터 자리를 잡고 겹치면 건너뛰는 방식, 확대/축소 시 재배치 |
| 5 | shp 내보내기 | 현재 화면 지표 기준으로 시도·시군구·값·순위·표준점수를 담은 ESRI Shapefile을 브라우저에서 생성해 저장. 외부 라이브러리 없이 직접 작성 |
| 6 | 성적표 정리 | 9등급 구분을 화면·문서 전체에서 제거. T점수(평균 50 · 표준편차 10) 표기와 설명 문구를 통일 |
| 7 | 산점도 추가 | 통계 패널에 축을 직접 고르는 산점도 추가. 지표·CI·T점수·순위 중 두 축을 골라 비교 |
| 8 | 문서 재작성 | 기능 명세서와 정보구조(I.A) 설계서를 새 구조로 다시 쓰고, 그에 맞춰 발표자료 본문을 전면 교체 |

### 부문별 표준화 민감도 (기본 조합 · 최근 연도 기준)

| 부문 | 지표 | 기준연도 | 평균 이동 | 최대 | 10계단 이상 |
|---|---|---|---|---|---|
| S1 공간구조 효율성 | 3 | 2025 | 9.3 | 35 | 96곳 (41.9%) |
| S2 인구활력도 | 6 | 2024·2025 | 16.6 | 73 | 147곳 (64.2%) |
| S7 친환경성 | 4 | 2024·2025 | 15.9 | 101 | 141곳 (61.6%) |
| S8 지역사회건강도 | 7 | 2023 | 9.2 | 50 | 92곳 (40.2%) |
| S9 지역사회안전 | 6 | 2024 | 5.2 | 26 | 37곳 (16.2%) |
| S10 재정건전성 | 6 | 2024 | 33.6 | 148 | 181곳 (79.0%) |

재정건전성은 229곳 중 181곳이 10계단 이상 움직입니다. 표준화 방법을 밝히지 않으면 이 부문의 순위는 해석하기 어렵습니다. 반대로 지역사회안전은 평균 5.2계단에 그쳐 방법을 바꿔도 순위가 거의 유지됩니다.

### 이번에 걸린 문제

| 문제 | 원인 | 조치 |
|---|---|---|
| 민감도 수치가 전 부문 동일하게 나옴 | 값이 `rows[i]`에 있다고 가정했으나 실제로는 `series[컬럼명]` 배열에 들어 있어 전부 NaN | 계열 배열에서 직접 읽도록 수정 |
| 지표를 바꿔도 화면이 갱신되지 않음 | 계산 결과를 공유 행 객체에 덮어써서 객체 주소가 그대로라 캐시가 무효화되지 않음 | 선택이 바뀔 때마다 증가하는 버전 값을 함께 넘겨 캐시를 끊음 |
| 빌드 실패 | JSX 주석이 `return (` 바로 뒤에 들어가 파서가 막힘 | 주석을 `return` 위로 이동 |
| 표지 사진이 주제와 무관 | 이전 자료의 임시 이미지가 남아 있었음 | 지도 화면 이미지로 교체 |

## 현재 상태

- 배포본: `docs/index.html` 단일 파일 4.0MB (JS·CSS·데이터 전량 인라인)
- Pages 설정: Settings → Pages → Branch `main` / `/docs`
- 자료 범위: 229개 시군구 · 10개 부문(가동 6) · 지표 32개 · 지표-연도 98열 · 2019~2025년
- 화면 상태 공유: URL 해시 9개 키(부문·방법·지표·담은 지표·시도·선택 지역·비교·패널·산점도 축)
- 검증: 실제 코드 오류 0건(지도 타일·웹폰트 CDN 요청 실패는 컨테이너 네트워크 차단에 의한 것), 화면 캡처 68장 전량 재촬영

## 남은 개선 여지

준비중 4개 부문(지역경제활력도·지역산업성장성·교통인프라 접근성·생활인프라 편리성) 자료 연결, 가중치 조정 시나리오, 민감도 상위 시군구 원인 진단, 연도별 시계열 추세 비교
