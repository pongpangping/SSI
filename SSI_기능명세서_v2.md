# 국토종합진단지수 진단도구 — 기능명세서 (v2)

작성일 2026-07-28 · 대상 화면: 국토종합진단지수 표준화 민감도 진단 웹

---

## 1. 이 문서의 범위

v1은 부문 두 개(S1·S8)에 지표가 각각 서너 개, 연도는 하나뿐이었다. 화면도 그 크기에 맞춰
지표를 선택 박스로 전부 늘어놓는 방식이었다.

v2에서 들어온 자료는 부문 여섯 개, 지표 서른두 개, 연도가 최대 일곱 개다. 지표·연도 조합만
100개에 가깝다. 이걸 그대로 늘어놓으면 화면이 목록으로 뒤덮인다. 그래서 v2의 기본 방향을
바꾼다.

> **화면이 지표를 늘어놓지 않는다. 사용자가 메타데이터를 보고 골라 오면, 화면은 고른 것만
> 계산해서 보여준다.**

이 한 줄이 아래 모든 변경의 이유다.

---

## 2. 자료 현황

### 2.1 부문

| 코드 | 부문 | 지표 수 | 연도 | 상태 |
|---|---|---|---|---|
| S1 | 공간구조 효율성 | 3 | 2021·2022·2023·2024·2025 | 사용 |
| S2 | 인구활력도 | 6 | 2020·2024·2025 | 사용 |
| S3 | 지역경제활력도 | 5 | 2024·2025 | 준비중 |
| S4 | 지역산업성장성 | 8 | 2018·2020·2022 | 준비중 |
| S5 | 교통인프라 접근성 | 6 | 2021~2023 | 준비중 |
| S6 | 생활인프라 편리성 | 5 | 2023·2024 | 준비중 |
| S7 | 친환경성 | 4 | 2019~2025 | 사용 |
| S8 | 지역사회건강도 | 7 | 2021·2022·2023 | 사용 |
| S9 | 지역사회안전 | 6 | 2021~2024 | 사용 |
| S10 | 재정건전성 | 6 | 2024 | 사용 |

준비중 4개 부문은 지표체계(참고1, 2025-07-21판)에 이름·설명·방향이 이미 잡혀 있으므로
화면에 자리를 만들어 두되 잠근다. 자료가 들어오면 적재 스크립트만 다시 돌리면 열린다.

### 2.2 자료 구조

시군구 229개 × 시도 17개. 지표 열 이름은 `S{부문}_{지표번호}_{연도 뒤 두 자리}` 규칙을
따른다. 예: `S8_1_23` = 지역사회건강도 1번 지표(사망률) 2023년.

부문별 파일에 딸린 메타데이터 시트가 지표명·연도·정의·산식·단위·출처를 갖고 있다.
화면이 읽는 설명은 전부 여기서 온다. 코드에 지표 이름을 적어 두지 않는다.

### 2.3 방향

지표마다 "높을수록 좋음(▲) / 낮을수록 좋음(▼)"이 다르다. S10만 자기 메타데이터에 방향
열이 있고, 나머지는 지표체계 파일에서 가져온다.

| 부문 | ▲ 높을수록 좋음 | ▼ 낮을수록 좋음 |
|---|---|---|
| S1 | 거점화율, 거점부 인구집중도 | 평균 출근거리 |
| S2 | 인구변화율, 청년 순이동률, 유입활동인구지수 | 청년인구 성비, 노령화지수, 전고령 과소격자 비중 |
| S7 | 신재생에너지 생산량 | 법제적 평가 등급, 환경생태적 평가 등급, 탄소 순배출량 |
| S8 | 주관적 건강인지율 | 사망률, 비만율, 암발생률, 고혈압 유병률, 당뇨 유병률, 의료이용미충족율 |
| S9 | — | 교통사고, 화재, 범죄, 생활안전, 자살, 감염병 |
| S10 | 통합재정수지비율 | 경상수지비율, 관리채무비율, 관리채무상환비율, 통합유동부채비율, 공기업부채비율 |

확인이 필요한 대목: 지표체계 파일에는 9.4 생활안전만 ▲로 적혀 있다. 지역안전지수는 여섯
영역 모두 1등급이 가장 안전한 같은 규칙이므로 ▼로 처리했다. 지표체계 쪽 오기로 보이며,
아니라면 한 줄만 고치면 된다.

### 2.4 값의 성격

S7의 1·2번과 S9 전체는 값 자체가 등급(1~5)이다. 연속량이 아니므로 표준화 방법에 따라
결과가 크게 흔들린다. 이 부문은 순위전용형(백분위 순위)이 더 안정적이라는 점을 화면에
안내한다.

S9·S10 자료에는 "제주특별자치도는 제주시·서귀포시에 같은 값을 부여함"이라는 단서가
붙어 있다. 두 시의 값이 같게 나오는 것은 오류가 아니므로 화면에서 참고 표시를 한다.

---

## 3. 계산 규칙

### 3.1 파이프라인

```
지표 원값 → 방향 반전(▼ 지표는 부호를 뒤집는다) → 표준화(0~100) → 동일가중 평균 = 부문점수(CI) → 전국순위
```

### 3.2 표준화 방법 네 가지

| 방법 | 구분 | 계산 | 성격 |
|---|---|---|---|
| Min-Max | 간격보존형 | (x − 최소) / (최대 − 최소) × 100 | 값의 간격을 그대로 살린다. 극단값에 끌려간다 |
| 거리기반 | 간격보존형 | x / 전국평균 × 100 | 100이 전국평균. 상한이 없다 |
| 로지스틱 | 간격보존형 | 1 / (1 + e^(−z)) × 100 | 가운데는 벌리고 양끝은 눌러 준다 |
| 백분위 순위 | 순위전용형 | 순위 / 전체 × 100 | 간격을 버리고 서열만 남긴다 |

두 진영의 대표는 Min-Max와 백분위 순위다. 이 둘의 부문 순위 차이가 "표준화 민감도"다.

```
민감도 = | Min-Max 기준 순위 − 백분위 순위 기준 순위 |
```

### 3.3 성적표 셈법

| 항목 | 계산 | 읽는 법 |
|---|---|---|
| 원점수 | 자료 그대로 | 지표의 실제 값 |
| 표준화 값 | 위 네 방법 중 하나 | 0~100 눈금 위의 자리 |
| 표준점수 (T) | 50 + 10z | **전국 평균이 50, 표준편차가 10.** 60이면 평균보다 1표준편차 위 |
| 백분위 | (전체 − 순위) / 전체 × 100 | 나보다 점수가 낮은 지역의 비율 |
| 전국순위 | 1 = 최상위 | 229개 중 몇 번째 |

**v1 대비 변경: 9등급 표기를 전부 뺀다.** 등급은 컷을 어디에 두느냐에 따라 한 계단이
왔다 갔다 하는데, 이 도구의 목적은 표준화 방법이 자리를 어떻게 흔드는지 보는 것이라 등급이
그 신호를 가린다. 순위·백분위·T점수로 충분하다.

T점수는 계산을 바꾸지 않는다(원래부터 평균 50·표준편차 10이다). 화면 문구만 100점 만점처럼
읽히지 않게 정리한다.

### 3.4 계산 시점

v1은 표에서 계산을 끝내고 결과만 화면에 실었다. v2는 사용자가 지표·연도를 고를 수 있으므로
**표준화·평균·순위를 화면에서 즉시 계산한다.** 자료 파일에는 원값과 메타데이터만 담는다.
229행 × 100계열은 브라우저에서 수 밀리초면 끝난다.

---

## 4. 기능 명세

### F1. 지표 고르기 (신규 — v1의 선택 박스 나열을 대체)

| 항목 | 내용 |
|---|---|
| 진입 | 상단 "지표 고르기" 버튼 또는 부문 이름 클릭 |
| 화면 | 왼쪽 부문 목록 / 가운데 지표 카드 / 오른쪽 담은 목록 |
| 지표 카드 | 지표명, 방향 표시, 단위, 정의, 산식, 출처, 사용 가능한 연도 칩 |
| 고르는 단위 | 지표 + 연도. 같은 지표의 다른 연도를 함께 담을 수 있다 |
| 검색 | 지표명·정의·출처를 한 칸에서 찾는다 |
| 기본값 | 부문을 열면 그 부문 전체 지표의 가장 최근 연도가 담긴 상태 |
| 되돌리기 | "기본 조합으로" 한 번에 복귀 |
| 잠김 | S3~S6은 이름과 설명만 보이고 담기 버튼이 비활성. "자료 준비중" 표시 |
| 결과 | 담은 조합으로 부문점수를 다시 계산하고 지도·순위·성적표가 함께 갱신 |

담은 지표가 0개면 계산하지 않고 안내를 띄운다. 1개면 그 지표 자체가 부문점수가 된다.

### F2. 연도 다루기 (신규)

| 항목 | 내용 |
|---|---|
| 연도 칩 | 지표 카드마다 그 지표에 실제로 있는 연도만 표시 |
| 여러 해 담기 | 같은 지표의 2021·2023을 각각 담으면 두 계열로 취급 |
| 연도 비교 | 담은 조합에서 연도만 바꾼 짝을 만들어 순위 이동을 본다 |
| 빈 연도 | 자료가 없는 시군구는 빈칸으로 두고 평균에서 제외 |

부문마다 연도가 제각각이라 "2023년 전국 종합"처럼 한 해로 자르지 않는다. 무엇을 골랐는지
화면 위에 항상 적어 둔다.

### F3. 지도

| 항목 | v1 | v2 |
|---|---|---|
| 경계 | 시군구 229 + 시도 17 | 동일 |
| 색칠 기준 | 부문점수·순위·T점수·백분위·등급·민감도 | **등급 제외**, 나머지 유지 + 담은 지표 개별 값 |
| 구간 나누기 | 등간·분위·자연구분·표준편차 자동 판단 | 동일 |
| **시군구 이름** | 없음 | **신규 — 경계 안쪽 중심에 이름 표시** |
| **내보내기** | PNG·CSV | **신규 — shapefile(zip)** |

**시군구 이름 표시(F3-a)**
- 켜고 끄는 버튼을 지도 오른쪽 위에 둔다. 기본값은 켜짐.
- 축척이 낮으면(전국 보기) 시도 이름만, 확대하면 시군구 이름이 나온다.
- 글자는 흰 테두리를 두른 검정으로 색칠 위에서도 읽히게 한다.
- 이름이 겹치면 면적이 큰 쪽을 남긴다.

**shapefile 내보내기(F3-b)**
- 현재 지도에 칠해진 그 지표 기준으로 내보낸다.
- 속성: 시도, 시군구, 값, 순위, 표준점수(T), 백분위, 표준화 방법.
- 좌표계 정보(.prj)를 함께 넣어 QGIS에서 바로 열리게 한다. EPSG:4326.
- .shp/.shx/.dbf/.prj/.cpg를 zip 하나로 묶어 내려받는다.
- 속성 이름은 dbf 규격상 10자 제한이라 영문 축약을 쓰고, 대응표를 zip 안 README에 넣는다.

### F4. 통계 창

| 화면 | v1 | v2 |
|---|---|---|
| 분포 비교 | 있음 | 유지 |
| 방법 간 순위 이동 | 있음 | 유지 |
| 표준화 변환 곡선 | 있음 | 유지 |
| 원자료 표 | 있음 | 유지(담은 지표만) |
| 민감도 목록 | 있음 | 유지 |
| **산점도** | 없음 | **신규** |

**산점도(F4-a)**
- X축·Y축을 각각 고른다. 고를 수 있는 것: 담은 지표(연도 포함), 부문점수, 순위, T점수.
- 점 하나가 시군구 하나. 고른 지역은 크게, 같은 시도는 진하게 표시한다.
- 상관계수(피어슨)와 추세선을 함께 그린다. 점 위에 마우스를 올리면 지역 이름이 나온다.
- 점을 누르면 그 지역이 선택된다(지도·성적표와 연동).
- 쓰는 곳: 두 지표가 같은 이야기를 하는지, 어느 지역이 두 축 모두에서 벗어나 있는지.

### F5. 성적표

- 부문 종합 한 줄 + 담은 지표별 한 줄.
- 각 줄에 원점수·표준화 값·표준점수(T)·순위·백분위. **등급 배지 삭제.**
- 하단 방법 전환 버튼은 순위만 보여준다(등급 칩 삭제).
- 내려받기 CSV에서도 `_등급` 열을 뺀다.

### F6. 나란히 비교

- 지도 두 장을 좌우로. 비교 축은 표준화 방법 / 부문 / 연도 / 지표 중에서 고른다.
- 연도 비교가 v2에서 새로 가능해진 축이다.

### F7. 그대로 두는 것

주소 해시로 화면 상태 복원, 시도 필터, 지역 검색, 용어집, 커서 효과, 접히는 사이드바.
지표 선택 상태도 해시에 실어 링크로 공유할 수 있게 한다.

---

## 5. 제거 목록

| 대상 | 위치 | 사유 |
|---|---|---|
| 9등급 산출 | report.js의 등급 계산·구분선·색 | 3.3 참조 |
| 등급 지도 | 지도 색칠 기준 목록 | 색 7단계와 등급 9단계가 어긋나 1·2등급이 같은 색이었다 |
| 등급 배지 | 성적표 상단·각 줄·방법 전환 버튼 | 위와 같음 |
| `_등급` 열 | 내려받기 CSV | 위와 같음 |
| 지표별 선택 박스 나열 | 사이드바 | F1이 대체 |

---

## 6. 화면에서 지켜야 할 것

1. 무엇을 골랐는지 항상 보인다. 지도 위 한 줄에 부문·방법·지표 수·연도가 적힌다.
2. 계산 결과보다 계산 방법이 먼저 보인다. 이 도구는 값을 알려주는 게 아니라 값이 방법에
   얼마나 기대고 있는지를 보여주는 도구다.
3. 자료가 없으면 없다고 쓴다. 0으로 채우거나 평균으로 메우지 않는다.
4. 준비중인 부문은 감추지 않고 잠근다. 전체 열 개 중 어디까지 왔는지가 보여야 한다.
