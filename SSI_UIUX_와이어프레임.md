# 국토종합진단지수 대시보드 — 정보 위계 · Lo-Fi 와이어프레임 · UX 병목 진단

기능 명세와 IA는 `SSI_기능명세서_IA.md`에 있다. 이 문서는 그 다음 단계인 **정보 계층화 → 선형 와이어프레임 → 병목 진단**을 다룬다.

---

## 0. 입력 정리

| 항목 | 내용 |
| --- | --- |
| 화면명 | 표준화 방법 민감도 진단 (단일 화면 대시보드) |
| 주요 기능 | 부문 선택(S1·S8) · 지도 색 기준 지표 선택(부문당 20개) · 행정구역 선택(시·도 → 시·군·구) · 표준화 방법 선택(4종) · 민감 지역만 보기 · 두 방식 나란히 비교 · 지도 확대/이동 · 선택 지역 판독 카드 9종 · 전체 데이터표 · 용어·방법론 |
| 타겟 사용자 | 국토·지역 정책 담당자와 연구자. 통계는 읽지만 표준화 기법 이름(Min-Max, 백분위순위)까지는 익숙하지 않다. 자기 지역 또는 담당 권역이 먼저다 |
| 목적 | "같은 자료인데 표준화 방법을 바꾸면 우리 지역 순위가 얼마나 흔들리는가"를 확인하고, 그 결과를 보고서에 인용 가능한 근거로 가져가는 것 |
| 이용 맥락 | 데스크톱 1440px 이상, 한 번에 10~30분, 지도–표–차트를 오가며 비교. 모바일은 부차 |

---

## 1. 프롬프트 1 — 핵심 정보와 CTA 우선순위, 블록 재배치

### 1.1 사용자가 얻어야 하는 핵심 정보

| 순위 | 핵심 정보 | 판단 근거 | 화면에서의 형태 |
| --- | --- | --- | --- |
| 1순위 | **선택한 시군구의 순위가 방법에 따라 몇 계단 움직이는가** | 이 서비스가 존재하는 이유. 다른 모든 수치는 이 한 값을 설명하기 위한 보조다 | 판독부 최상단 카드의 `MinMax 39위 ↕ 16계단 ↓ PctRank 55위`, 지도 위 실시간 띠의 `순위 이동 16계단` |
| 2순위 | **전국 229개 안에서 그 지역이 어디쯤인가** | 이동 폭만으로는 크고 작음을 판단할 수 없다. 분포 안의 위치가 있어야 해석이 된다 | 지도 색(등급), 요약 4칸(평균 이동 5.2 · 10계단↑ 35곳 · 민감 50곳), 민감도 산점도 |
| 3순위 | **왜 그렇게 움직였는가** (원자료 → 표준화 → CI → 순위) | 보고서에 인용하려면 근거가 필요하다. 다만 필요할 때만 찾는 정보라 위계는 3순위 | 계산 과정 표, 방법별 분포 비교, 범프 차트 |

### 1.2 가장 먼저 취해야 하는 행동(CTA)

| 순위 | 행동 | 성격 | 배치 원칙 |
| --- | --- | --- | --- |
| 1순위 | **표준화 방법 바꾸기** (4눈금 축 슬라이더) | 이 화면의 정체성. 조작 → 지도 색과 순위가 즉시 바뀌는 것을 보는 것이 학습의 핵심 | 조작부 중앙, 눈으로 지도를 보면서 손이 닿는 높이 |
| 2순위 | **볼 지역 좁히기** (시·도 → 시·군·구) | 사용자는 언제나 자기 지역부터 찾는다. 다만 방법을 바꾸지 않으면 화면의 의미가 없으므로 2순위 | 조작부, 방법 축 바로 위 |
| 3순위 | **두 방식 나란히 비교하기** | 1순위 조작을 한 번 이상 해 본 뒤에야 필요해지는 심화 행동. 화면 구조를 바꾸는 유일한 버튼 | 조작부 고정 발판에 전폭 Primary 버튼 하나 |

즉시 반영형(부문·지표·방법·지역)은 버튼을 두지 않는다. "적용" 버튼은 이미 반영된 결과를 다시 누르게 만들어 조작 신뢰를 떨어뜨린다.

### 1.3 시선 흐름에 맞춘 블록 재배치

읽는 순서 = 인과 순서. **고른다 → 읽는다 → 본다**를 좌에서 우로 눕히되, 각 열 안에서는 위에서 아래로 내려간다.

| 블록 | 위치 | 담는 것 | 우선순위 근거 |
| --- | --- | --- | --- |
| Header | 최상단 전폭 58px | 로고·제목 / (우) 용어·방법론, 전체 데이터표 | 화면 정체성과 탈출구만. 수치는 두지 않는다 |
| Sidebar (조작부) | 좌 300px | ① 무엇을 볼까(부문 아코디언) ② 어떤 조건으로(지역·방법·범례) | CTA 1·2순위가 모두 여기 있다 |
| Sidebar Footer (고정) | 좌 하단 | 요약 4칸 + Primary CTA 1개 | 3순위 CTA를 시선의 끝점에 고정 |
| Main A (판독부) | 중앙 366px | 흐름 막대 → A 이 지역은 어떤가 → B 방법이 만든 차이 → C 전국 속 위치 | 핵심 정보 1순위가 A 첫 카드 |
| Main B (지도) | 우 가변 | 전국 코로플레스 + 조작 스택(우상) + 보기 옵션(우하) + 실시간 띠(좌하) | 핵심 정보 2순위. 가장 넓은 면적 |
| Footer | 없음 | — | 단일 화면 도구에 하단 푸터는 스크롤 비용만 만든다. 요약·출처는 조작부 발판과 헤더로 흡수 |

```
┌──────────────────────────────────────────────────────────────┐
│ HEADER  로고·제목                       용어·방법론 │ 데이터표 │
├────────────┬──┬────────────────┬──┬───────────────────────────┤
│ SIDEBAR    │조│ MAIN A 판독부  │판│ MAIN B 지도               │
│ ① 무엇을 볼까│작│ 흐름막대 1›2›3›4│독│  ┌ ＋ － ↺ ◎ ⤢ (우상)     │
│  부문 아코디언│  │ A 이 지역은…  │  │                           │
│ ② 어떤 조건으로│ │ B 방법이 만든…│  │      전국 코로플레스       │
│  지역 · 방법축│  │ C 전국 속 위치│  │                           │
│ ┄┄┄┄┄┄┄┄┄ │  │                │  │ 실시간 띠 ┄┄ 범례 · 보기옵션│
│ 요약 4칸    │  │                │  │                           │
│ [ Primary CTA ]│ │                │  │                           │
└────────────┴──┴────────────────┴──┴───────────────────────────┘
```

---

## 2. 프롬프트 2 — Lo-Fi 텍스트 와이어프레임

### 2.1 그리드와 시선 경로

| 항목 | 결정 |
| --- | --- |
| 그리드 | 4구획 수평 분할 — 조작 300px 고정 / 손잡이 20px / 판독 366px 고정 / 지도 가변(최소 420px). 여백 14px 단일 값, 모서리 반경 10~12px 두 단계만 사용 |
| 주 시선 경로 | **F-pattern**. 조작부(세로 스캔) → 판독부 상단(가로 스캔) → 지도(체류). 대시보드는 랜딩이 아니라 작업 도구이므로 Z-pattern의 대각 도약보다 좌측 세로축 반복 스캔이 실제 행동에 맞는다 |
| 보조 시선 경로 | 지도 안에서는 **Z-pattern**. 좌상 지도 제목 → 우상 확대 스택 → 좌하 실시간 띠 → 우하 범례·보기 옵션. 네 모서리에 각각 하나씩만 두어 가운데(지도 본체)를 비운다 |
| 위계 단계 | 3단만 — 구획 캡션(12px/750) · 카드 제목(13px/750) · 본문(11.5px/600). 4단째를 만들지 않는다 |
| 색 규칙 | 파랑=간격보존형·현재 선택, 주황=순위전용형·민감, 먹색=활성 카드 머리말. 이 3색 외에는 무채색 |

### 2.2 블록별 와이어프레임

```
[상단 헤더]  높이 58px · 배경 흰색 · 하단 1px 실선
 - 왼쪽: 로고 마크(32px 먹색 라운드, 'SSI') + 제목 "국토종합진단지수 · 표준화 방법 민감도 진단"
 - 중앙: (비움) — GNB 없음. 단일 화면 도구이므로 이동 메뉴를 두지 않는다
 - 오른쪽: [ⓘ 용어·방법론] 보조 버튼 / [▤ 전체 데이터표] 보조 버튼
 - 가이드라인: 헤더에 수치·칩을 두지 않는다. 같은 수치는 조작부 발판에 이미 있다
```

```
[사이드바 블록 1: 무엇을 볼까]  구획 캡션 + 아코디언
 - 캡션 왼쪽: 번호 배지 ①(먹색 사각) + "무엇을 볼까"
 - 캡션 오른쪽: 부제 "부문 · 지도 색 기준" (10px 회색)
 - 컴포넌트: 아코디언 카드 2장
   · 닫힘: 흰 배경 / 회색 아이콘 / 제목 + 코드(S1) / 오른쪽 [+]
   · 열림: 먹색 머리말 / 흰 글씨 / 오른쪽 [−] / 본문 흰 배경
 - 열린 본문 내용:
   · 지표 칩 줄 — 부문 구성 지표(예: 거점화율, 거점부 인구집중도)
   · 그룹 소제목(표준화 결과 / 민감도 / 원자료 지표 / 지표별 표준화값 / 파생 플래그)
   · 지표 항목 목록 — 선택 항목은 연파랑 배경 + 왼쪽 3px 파랑 막대
   · 방법에 따라 값이 바뀌는 지표에는 ◆ 표식
 - 가이드라인: 본문 최대 높이 300px, 넘치면 카드 안에서만 스크롤.
   아래 구획이 화면 밖으로 밀리지 않게 하는 것이 이 규칙의 목적
```

```
[사이드바 블록 2: 어떤 조건으로]  구획 캡션 + 컨트롤 줄
 - 캡션: 번호 배지 ② + "어떤 조건으로" / 오른쪽 부제 "범위 · 점수 매기는 법"

 - 컨트롤 줄 A: 시 · 도
   · 평상시: [ › ] 라벨 "시 · 도" ······················ 값 "전국" (오른쪽 정렬 굵게)
   · 펼침: 줄 아래로 3열 칩 격자 — [전국 229] [서울 25] [부산 16] … 각 칩에 시군구 수 병기
   · 선택 칩: 파랑 배경 흰 글씨
 - 연결선: 두 줄 사이 왼쪽 17px 지점에 2px 세로선 (잠김이면 회색)
 - 컨트롤 줄 B: 시 · 군 · 구
   · 평상시: [ › ] "시 · 군 · 구" ················ 값 "관악구"
   · 잠김: 값 자리에 "시 · 도부터" 회색, 줄 아래 "시 · 도를 먼저 고르세요"
   · 펼침: 9개 이상이면 검색칸 먼저, 그 아래 목록. 선택 항목은 왼쪽 3px 파랑 막대
   · 연쇄: 시·도 칩을 고르면 이 줄이 자동으로 열린다
 - 보조 줄: "지도 전국 · 목록 ○○ 기준" 또는 [지도 ○○만 보는 중 · 전국으로] 점선 버튼

 - 소제목: "표준화 방법"
 - 컨트롤 C: 4눈금 축 슬라이더
   · 레일: 4점 눈금, 채움은 선택 위치까지 계열색
   · 눈금 라벨: Min-Max / 거리기반 / 로지스틱 / 백분위순위 (라벨 자체가 클릭 영역)
   · 축 양끝 설명: 왼쪽 "값 간격 그대로" · 오른쪽 "등수만"
   · 조작: 클릭 · 드래그 · 키보드 방향키 모두 동일 동작
 - 상세 카드(축 아래): 계열 배지(간격/등수) + 방법명 + 눈금 띠 + 수식 + 범위
 - 전환 비용 3칸: [MM → DI  12곳 색 바뀜] [MM → PR  45곳] [MM → LG  8곳]
 - 색 범례: "낮음 ─────── 높음"
 - 안내 한 줄: "민감 지역만 보기 스위치는 지도 오른쪽 아래에 있습니다"
```

```
[사이드바 고정 발판]  스크롤과 무관하게 항상 보이는 하단
 - 상단: 요약 4칸 — 시군구 229 / 평균 이동 5.2 / 10계단↑ 35 / 민감 50
 - 하단: 주 버튼(Primary CTA) 전폭 —
   · 기본: 연파랑 배경 · 파란 글씨 "두 방식 나란히 비교하기"
   · 활성: 진한 파랑 배경 · 흰 글씨 "비교 끝내고 한 지도로"
 - 버튼 위치: 좌측 열의 최하단. F-pattern 세로 스캔의 종착점
 - 가이드라인: 이 화면의 Primary는 영원히 하나. 두 번째 채운 버튼을 만들지 않는다
```

```
[패널 손잡이]  조작부 오른쪽 · 판독부 오른쪽, 각 20px
 - 구성: 세로 정립 글씨 라벨("조작" / "판독") + 방향 셰브런
 - 열림: 흰 배경 회색 글씨 / 접힘: 먹색 배경 흰 글씨(반전)
 - 가이드라인: 패널을 접어도 손잡이는 같은 자리에 남는다. 접기와 펼치기의 클릭 지점이 동일
```

```
[메인 A: 판독부]  366px 고정 · 세로 스크롤
 - 최상단(고정): 흐름 막대 — ①서울특별시 관악구 › ②공간구조효율성 › ③Min-Max › ④전국 순위
   · 마지막 칩만 강조색. 지금 화면이 어떤 선택의 결과인지 한 줄로 되돌려준다
 - 구간 A "이 지역은 어떤가" — 선택한 시군구
   · 카드 A-1 표준화 방법별 점수와 순위 (핵심 정보 1순위가 여기)
     상단: 지역명 + [순위 이동 16계단 · 민감] 배지
     중단: 간격보존형 39위 ↕ 순위 차 16계단 ↓ 순위전용형 55위
     하단: 4방법 × (CI 점수 / 전국 순위) 표, 현재 방법 행 강조
   · 카드 A-2 부문 안의 원자료 지표 — 값 + 전국 상위 % 막대
 - 구간 B "방법이 만든 차이" — 계산 과정 / 같은 자료 다른 모양 / 순위 이동 범프 차트
 - 구간 C "전국 속 위치" — 민감도 산점도 / 순위 이동이 큰 시군구 목록
 - 가이드라인: 설명 문단을 쓰지 않는다. 문장으로 쓰고 싶은 내용은 배지·칩·표로 바꾸고,
   원래 문장은 title 속성으로 옮겨 필요할 때만 나타나게 한다
```

```
[메인 B: 지도]  가변 폭 · 네 모서리에 하나씩
 - 좌상단: 지도 제목(나란히 보기일 때만)
 - 우상단: 확대·이동 스택 — [＋] [－] ─ [↺ 전국] [◎ 선택 시·도] [⤢ 선택 시군구]
   · 조건이 없는 버튼은 비활성 회색(사라지지 않는다 — 위치 기억을 깨지 않기 위해)
 - 좌하단: 실시간 판독 띠 — "서울특별시 관악구 · 전국 순위 39위 · 순위 이동 16계단 [민감]"
   · 커서 아래 지역이면 파란 배경, 커서를 빼면 선택 지역으로 복귀하며 먹색 배경
 - 우하단: 보기 옵션 — [ ○ OFF · 민감 지역만 ] [초기화]
 - 우하단 위: 색 범례 — 지표명 + 7단 색띠 + 양끝 라벨
 - 가운데: 비운다. 어떤 패널도 지도 본체를 덮지 않는다
 - 나란히 보기: 두 지도 사이 상단 가운데에 공용 확대 스택, 하단 가운데에 공용 보기 옵션.
   좌우 지도의 범례와 겹치지 않는 유일한 자리
```

---

## 3. 프롬프트 3 — UX 병목 3가지와 개선안

진단 대상은 2차 개편 시점의 구도(조작부 1~5단계 카드 · 네이티브 셀렉트 2개 · 방법 카드 4장 · 좌상단 지도 도구막대 · 판독부 접기 레일 1개)이다.

### 3.1 병목 ① 시각적 정보 절제 — 조작부가 "다섯 개의 대등한 카드"로 보인다

| 항목 | 내용 |
| --- | --- |
| 증상 | 1~5단계 카드가 같은 크기·같은 테두리·같은 배경으로 세로로 늘어서, 어디부터 만져야 하는지 판단에 3~5초가 걸린다. 세로 길이가 화면을 넘어 5단계는 스크롤해야 나타난다 |
| 원인 | 번호는 순서를 말하지만 **형태가 위계를 말하지 않았다.** 다섯 카드의 시각적 무게가 같으면 번호는 장식이 된다 |
| 개선안 A | 5단계를 2구획으로 접는다. "부문"과 "지도 색 기준"은 원래 한 질문이므로 아코디언 한 개로 합치고, 지금 필요 없는 부문의 지표 20개는 접어 감춘다 |
| 개선안 B | 형태로 상태를 말한다 — 열린 카드는 먹색 머리말과 `−`, 닫힌 카드는 흰 머리말과 `+`. 화면에서 검은 덩어리는 언제나 한 개뿐이므로 시선이 그리로 먼저 간다 |
| 개선안 C | 아코디언 본문에 최대 높이(300px)를 주어 그 안에서만 스크롤시킨다. 목록이 길어져도 아래 구획과 CTA가 화면 밖으로 밀리지 않는다 |
| 개선안 D | 중복 조작 제거 — `민감 지역만` 스위치를 조작부에서 빼고 지도 우하단 한 곳에만 둔다. 조작부에는 위치를 알리는 9.5px 한 줄만 남긴다 |
| 적용 결과 | 조작부에 동시에 보이는 조작 요소가 11개 → 6개. 구획 ②와 CTA가 첫 화면 안에 들어온다 |

### 3.2 병목 ② 접근성과 동선의 연속성 — 종속 관계가 조작에 드러나지 않는다

| 항목 | 내용 |
| --- | --- |
| 증상 | 시·도와 시·군·구가 같은 모양의 네이티브 셀렉트 두 개라 대등해 보인다. 시군구부터 열어 보는 시도가 반복되고, 셀렉트를 열면 OS 팝업이 화면 밖으로 튀어나와 지도를 덮는다. 시·도를 고른 뒤 다음 손동작이 어디인지 안내가 없다 |
| 원인 | 종속 관계를 **문구로만** 표시하고 조작의 형태·순서로는 표시하지 않았다. 클릭 후 다음 목표까지 커서가 이동해야 할 거리도 길다 |
| 개선안 A | 인라인 확장 컨트롤로 교체 — 평상시엔 `› 라벨 ···· 값` 한 줄, 누르면 **그 줄 바로 아래**가 열린다. 커서 이동 거리가 줄 높이(38px) 안으로 줄고, 팝업이 지도를 덮지 않는다 |
| 개선안 B | 자동 연쇄 — 시·도 칩을 고르는 순간 시·군·구 줄이 스스로 열린다. "다음은 여기"를 말하지 않고 보여준다 |
| 개선안 C | 잠금의 시각화 — 범위가 없으면 시·군·구 줄을 비활성으로 두고 두 줄을 잇는 세로 연결선을 회색으로 끊는다. 조작 가능 여부가 색과 선으로 읽힌다 |
| 개선안 D | 클릭 영역 확대 — 시·도는 3열 칩 격자(각 칩 88×40px, 시군구 수 병기), 시·군·구는 목록 항목 전체가 클릭 영역(38px 높이). 9개 이상이면 검색칸이 함께 열려 스크롤 대신 타이핑으로 좁힌다 |
| 개선안 E | 조작을 결과 옆에 — 지도 관련 옵션(민감 지역만·초기화)은 지도 위로 옮긴다. 지도를 보다가 조작부까지 커서를 왕복시키지 않는다 |
| 적용 결과 | 시·도 → 시·군·구 선택이 2클릭·커서 이동 40px 이내로 완료. 어떤 상태에서도 229개가 한 목록에 나열되지 않는다 |

### 3.3 병목 ③ 제품 조작부형 인터랙션 — 네 방법이 "네 개의 버튼"일 뿐이다

| 항목 | 내용 |
| --- | --- |
| 증상 | 표준화 방법 4종이 대등한 카드 4장이라, 방법 사이에 순서나 성질의 축이 있다는 사실이 보이지 않는다. 사용자는 하나씩 눌러 보며 차이를 기억으로 비교해야 한다. 이 화면의 주제가 "방법에 따라 결과가 어떻게 달라지는가"인데 그 주제가 조작의 형태에 없다 |
| 원인 | 연속적인 성질(값 간격을 얼마나 보존하는가)을 이산적인 선택지로 표현했다. 실물 제품이라면 스위치 4개가 아니라 **다이얼 하나**로 만들 자리다 |
| 개선안 A | 4눈금 축 슬라이더로 교체 — `값 간격 그대로 ── Min-Max ─ 거리기반 ─ 로지스틱 ─ 백분위순위 ── 등수만`. 손잡이를 훑으면 지도 색이 연속으로 변해 "방법 = 축 위의 위치"가 몸으로 이해된다 |
| 개선안 B | 조작 경로 다중화 — 눈금 점 클릭 · 라벨 클릭 · 드래그 · 키보드 방향키가 모두 같은 동작. 실물 다이얼처럼 어느 쪽으로 만져도 반응한다 |
| 개선안 C | 되먹임(feedback)을 조작 옆에 — 손잡이와 채움에 계열색(파랑/주황)을 입히고, 축 바로 아래에 고른 방법의 수식·범위·눈금 띠를, 그 아래에 "다른 방법으로 옮기면 몇 곳의 색이 바뀌는가" 3칸을 둔다. 조작의 결과를 예고한다 |
| 개선안 D | 지도 조작부를 네 모서리에 하나씩 — 우상 확대 스택(＋/－/↺/◎/⤢), 우하 보기 옵션, 좌하 실시간 판독 띠, 좌상 제목. 기기의 조작면처럼 기능군마다 고정된 자리를 준다. 조건이 없는 버튼은 사라지지 않고 비활성으로 남아 위치 기억을 깨지 않는다 |
| 개선안 E | 손잡이 불변 — 패널 접기 손잡이를 패널 바깥에 두어 접어도 같은 자리에 남기고, 접힘은 색 반전으로 표시한다. "접었더니 여는 법을 모르겠다"가 구조적으로 생기지 않는다 |
| 개선안 F | 주 행동은 하나 — 고정 발판의 전폭 버튼(`두 방식 나란히 비교하기`)만 채운 버튼으로 두고, 나머지 조작은 모두 즉시 반영형으로 유지한다. 이미 반영된 결과에 "적용"을 누르게 하지 않는다 |
| 적용 결과 | 방법 전환이 1조작(드래그·방향키)으로 연속 수행 가능. 지도 조작 요소가 좌상단 1군집 → 네 모서리 4군집으로 분산되어 지도 본체를 가리지 않는다 |
