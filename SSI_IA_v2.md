# 국토종합진단지수 진단도구 — 화면·정보 구조(IA) v2

기능명세서 v2에 따른 구조 설계. 2026-07-28.

---

## 1. 구조가 바뀌는 이유

v1의 사이드바는 부문을 펼치면 그 안에 지도 색 기준이 여남은 개씩 들어 있는 아코디언이었다.
부문이 둘일 때는 성립하지만, 열 개가 되면 목록이 목록을 낳는다. 세어 보면 부문 10 × (종합
지표 5 + 지표별 항목 최대 8) = 130줄짜리 아코디언이 된다. 아무도 끝까지 스크롤하지 않는다.

그래서 **선택하는 일과 보는 일을 분리한다.**

| | v1 | v2 |
|---|---|---|
| 고르는 곳 | 사이드바 아코디언 (상시 노출) | 지표 고르기 창 (필요할 때 연다) |
| 사이드바 | 부문 + 지표 + 색 기준 + 지역 + 방법 | 담은 조합 요약 + 색 기준 + 지역 + 방법 |
| 목록 길이 | 부문 수에 비례해 무한정 | 담은 지표 수에 비례 (보통 3~7) |

---

## 2. 화면 계층

```
앱
├─ 상단 머리줄
│   ├─ 제목 · 부제
│   ├─ [지표 고르기]        ← 신규. 선택 창을 연다
│   ├─ [전체 표]
│   └─ [용어집]
│
├─ 본문
│   ├─ 덱 (지도 위에 떠 있는 조작 묶음)
│   │   ├─ 조작부 (상시)
│   │   │   ├─ 1  담은 조합         ← 신규 요약 블록
│   │   │   │     부문 이름 · 지표 n개 · 연도 범위 · [고치기]
│   │   │   ├─ 2  지도 색 기준
│   │   │   │     부문 종합 / 담은 지표 / 표준화 민감도
│   │   │   ├─ 3  지역 범위
│   │   │   │     시도 · 시군구 고르기
│   │   │   ├─ 4  표준화 방법
│   │   │   │     4개 방법 + 범례
│   │   │   └─ 발판: 현황 수치 + [두 방식 나란히 비교]
│   │   │
│   │   └─ 통계창 (접힘 가능)
│   │       ├─ A 선택 지역
│   │       ├─ B 표준화 방법 비교
│   │       └─ C 전국 분포
│   │
│   ├─ 손잡이 (통계창 여닫기)
│   └─ 지도
│       ├─ 색칠된 시군구 · 시도 경계
│       ├─ 시군구 이름 라벨          ← 신규
│       ├─ 오른쪽 위: [이름 표시] [내보내기 ▾]   ← 내보내기 신규
│       ├─ 오른쪽 아래: [민감 지역만]
│       └─ 왼쪽 아래: 범례 · 구간 나누기 방식
│
├─ 지표 고르기 창 (모달)              ← 신규
├─ 전체 표 창 (모달)
└─ 용어집 창 (모달)
```

---

## 3. 지표 고르기 창

전체 화면을 덮는 3단 구성. 여기서 나가면 지도·통계가 새 조합으로 다시 그려진다.

```
┌───────────────────────────────────────────────────────────────────┐
│  지표 고르기                          [검색: 지표명·정의·출처]  ✕ │
├──────────────┬────────────────────────────────┬───────────────────┤
│ 부문 10       │  지표 카드 목록                 │ 담은 목록         │
│              │                                │                   │
│ ● S1 공간구조 │ ┌────────────────────────────┐ │ S8 지역사회건강도 │
│   S2 인구활력 │ │ 거점화율            ▲ 높을수록│ │ ─────────────── │
│ 🔒 S3 지역경제│ │ 거점부 면적 비중 · 단위 %    │ │ 사망률      2023 ✕│
│ 🔒 S4 산업성장│ │ 출처 국토모니터링             │ │ 비만율      2023 ✕│
│ 🔒 S5 교통접근│ │ 연도 [2021][2023][2025]     │ │ 암발생률    2023 ✕│
│ 🔒 S6 생활인프│ └────────────────────────────┘ │ 고혈압      2023 ✕│
│   S7 친환경성 │ ┌────────────────────────────┐ │ 당뇨        2023 ✕│
│ ● S8 건강도   │ │ 평균 출근거리        ▼ 낮을수록│ │ 미충족율    2023 ✕│
│   S9 지역안전 │ │ …                           │ │ 건강인지율  2023 ✕│
│   S10 재정    │ │ 연도 [2022][2024][2025]     │ │                   │
│              │ └────────────────────────────┘ │ 7개 · 2023년      │
│              │                                │ [기본 조합으로]   │
├──────────────┴────────────────────────────────┴───────────────────┤
│  담은 지표로 부문점수를 다시 계산합니다              [적용하고 닫기]│
└───────────────────────────────────────────────────────────────────┘
```

**동작 규칙**

| 상황 | 처리 |
|---|---|
| 연도 칩을 누른다 | 그 지표+연도가 담긴다. 다시 누르면 빠진다 |
| 지표 카드 제목을 누른다 | 가장 최근 연도가 담긴다 |
| 잠긴 부문(S3~S6) | 카드는 보이되 회색, 연도 칩 없음, "자료 준비중" 띠 |
| 다른 부문으로 이동 | 담은 목록은 부문별로 나뉘어 유지된다 |
| 담은 것이 0개 | 적용 버튼 비활성 + "지표를 하나 이상 담아 주세요" |
| 기본 조합으로 | 그 부문 전체 지표의 최신 연도로 되돌린다 |
| 검색 | 지표명·정의·산식·출처를 한 번에 훑는다. 부문 경계를 넘어 찾는다 |

**부문 목록의 표시**

| 표시 | 뜻 |
|---|---|
| ● | 지금 보고 있는 부문 |
| 숫자 배지 | 그 부문에서 담은 지표 수 |
| 🔒 | 자료 준비중 |

---

## 4. 조작부 1번 — 담은 조합 요약

지표 고르기 창을 닫으면 사이드바 맨 위가 이 상태를 되비춘다.

```
┌──────────────────────────────────┐
│ 1  담은 조합              [고치기] │
│ ┌──────────────────────────────┐ │
│ │ ✚ 지역사회건강도  S8          │ │
│ │ 지표 7개 · 2023년             │ │
│ │ 사망률 · 비만율 · 암발생률 …  │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

부문 전환은 이 카드를 눌러도 되고, 지표 고르기 창의 왼쪽 목록에서 해도 된다. 부문 열 개를
사이드바에 세로로 세우지 않는다.

---

## 5. 조작부 2번 — 지도 색 기준

담은 지표 수만큼만 늘어난다. 등급 항목은 없다.

| 묶음 | 항목 |
|---|---|
| 부문 종합 | 부문점수(CI) · 전국순위 · 표준점수(T) · 백분위 |
| 표준화 민감도 | 두 진영 순위 차이 · 순위 이동 방향 |
| 담은 지표 | 지표별 원값 / 표준화 값 / 순위 (담은 것만, 연도 표기와 함께) |

---

## 6. 통계창 세 단

| 단 | 이름 | 카드 | 변경 |
|---|---|---|---|
| A | 선택 지역 | 성적표 / 방법별 점수·순위 / 원자료 지표 | 등급 열·배지 삭제 |
| B | 표준화 방법 비교 | 계산 과정 / 방법별 분포 / 방법 간 순위 이동 | 유지 |
| C | 전국 분포 | **산점도(신규)** / 민감도 산점도 / 순위 이동 큰 곳 | 산점도 추가 |

C단 맨 위에 오는 새 산점도는 축을 직접 고른다.

```
┌───────────────────────────────────────┐
│ 산점도                                 │
│ X [사망률 2023        ▾]               │
│ Y [주관적 건강인지율 2023 ▾]           │
│                                       │
│    ·  ·   ·        r = −0.62          │
│  ·   ●·  ·  ·      점 = 시군구 229개  │
│    · ·  ·   ·      ● = 고른 지역      │
│  ─────────────                        │
└───────────────────────────────────────┘
```

기존 "민감도 산점도"는 Min-Max 순위 × 백분위 순위 고정 축이라 그대로 두고, 새 산점도가
자유 축을 맡는다.

---

## 7. 지도 위 조작

| 자리 | 요소 | 상태 |
|---|---|---|
| 오른쪽 위 | 이름 표시 켜기/끄기 | 신규 · 기본 켜짐 |
| 오른쪽 위 | 내보내기 ▾ (PNG · CSV · Shapefile) | Shapefile 신규 |
| 오른쪽 아래 | 민감 지역만 보기 | 유지 |
| 왼쪽 아래 | 범례 + 구간 나누기 방식 | 유지 |
| 지도 상단 띠 | 부문 · 방법 · 지표 n개 · 연도 | 신규 문구 |

**이름 라벨 규칙**

| 축척 | 표시 |
|---|---|
| 전국 (zoom ≤ 7) | 시도 이름만 |
| 중간 (zoom 8~9) | 인구 상위 시군구 |
| 확대 (zoom ≥ 10) | 시군구 전체 |

---

## 8. 상태와 주소

주소 해시에 담기는 항목.

| 키 | 뜻 | 예 |
|---|---|---|
| `s` | 부문 | `S8` |
| `m` | 표준화 방법 | `minmax` |
| `k` | 지도 색 기준 | `ci` |
| `i` | **담은 지표 목록 (신규)** | `S8_1_23.S8_2_23.S8_7_23` |
| `g` | 시도 | `경기도` |
| `r` | 고른 시군구 | `경기도\|성남시` |
| `c` | 나란히 비교 | `1` |
| `p` | 통계창 열림 | `0` |
| `x`,`y` | **산점도 축 (신규)** | `S8_1_23`, `ci` |

`i`가 없으면 그 부문의 기본 조합(전체 지표 최신 연도)으로 연다. 이 규칙 덕분에 기존 링크가
그대로 살아 있다.

---

## 9. 자료 계층

```
ssi.json
├─ meta        기준일 · 시군구 수 · 출처
├─ sectors     부문 10개 (준비중 4개 포함) · 이름 · 지표 목록 · 사용 가능 여부
├─ indicators  지표 32개 · 방향 · 단위 · 정의 · 산식 · 출처 · 사용 가능 연도
├─ series      지표+연도별 229개 원값        ← 계산의 원천
└─ rows        시군구 229개 · 시도 · 이름 · 코드
```

v1은 표준화·평균·순위까지 끝낸 결과를 파일에 담았다. v2는 **원값과 설명만 담고 계산은
화면에서 한다.** 사용자가 조합을 바꿀 때마다 파일을 다시 만들 수 없기 때문이다.

계산은 담은 조합이 바뀔 때 한 번 돌고 결과를 기억해 둔다. 229행 × 지표 7개 × 방법 4개는
브라우저에서 한 자릿수 밀리초다.

---

## 10. 만들고 고치는 파일

| 구분 | 파일 | 하는 일 |
|---|---|---|
| 신규 | `scripts/build_data2.py` | 부문 6개 + 지표체계를 읽어 ssi.json 생성 |
| 신규 | `src/lib/compute.js` | 담은 조합 → 표준화 → CI → 순위 (화면 계산) |
| 신규 | `src/lib/shpout.js` | 지도 결과를 shapefile(zip)로 |
| 신규 | `src/components/IndicatorPicker.jsx` | 지표 고르기 창 |
| 신규 | `src/components/PickedSummary.jsx` | 조작부 1번 담은 조합 카드 |
| 신규 | `src/components/ScatterPlot.jsx` | 자유 축 산점도 |
| 수정 | `src/lib/ssi.js` | 원값 파일 읽기 + 계산 계층 연결, 등급 제거 |
| 수정 | `src/lib/report.js` | 등급 관련 전부 삭제 |
| 수정 | `src/components/Sidebar.jsx` | 아코디언 → 요약 카드 + 색 기준 |
| 수정 | `src/components/NationalMap.jsx` | 이름 라벨 · 내보내기 메뉴 |
| 수정 | `src/components/ReportCard.jsx` | 등급 삭제, T 설명 정리 |
| 수정 | `src/components/CenterPanel.jsx` | 산점도 카드 추가 |
| 수정 | `src/App.jsx` | 담은 조합 상태 · 해시 키 확장 |
